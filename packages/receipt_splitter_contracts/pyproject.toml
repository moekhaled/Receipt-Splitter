[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "receipt-splitter-contracts"
version = "0.1.0"
description = "Shared contracts (intents + schemas) for Receipt Splitter services"
requires-python = ">=3.10"
dependencies = ["jsonschema>=4.18.0"]
